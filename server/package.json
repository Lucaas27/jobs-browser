{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "dev": "npx tsc --noEmit && tsx watch -r dotenv/config --no-warnings src/app.ts ",
    "start": "NODE_ENV=production node dist/app.js",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lucas Gomes",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.8",
    "dotenv": "^16.4.5",
    "tsx": "^4.9.0",
    "typescript": "^5.4.5"
  }
}
